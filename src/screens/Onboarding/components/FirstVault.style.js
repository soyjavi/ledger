import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  form: {
    alignItems: 'center',
  },

  caption: {
    textAlign: 'center',
  },

  button: {
    marginHorizontal: 'auto',
  },
});
