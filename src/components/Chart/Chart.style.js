import { StyleSheet } from 'react-native';

import { THEME } from '../../reactor/common';

const { UNIT } = THEME;

export default StyleSheet.create({
  title: {
    textAlign: 'right',
    marginBottom: UNIT / 4,
  },
});
